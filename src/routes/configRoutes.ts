// src/routes/configRoutes.ts

 

import express, { Request, Response } from 'express';
import { validateConfigData } from '../middleware/validations/validationMiddleware';

const router = express.Router();


export default router;